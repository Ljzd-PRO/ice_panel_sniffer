# 一次制冰机五线控制面板的逆向分析：从两张照片到可远程控制

这是一篇完整的实验复盘。

对象是一台家用制冰机的控制面板。它的面板上有 5 个指示灯、2 个按钮，但连接到主控板的排线只有 5 根。我们最终用一块 ESP32-C3 开发板，在不拿到厂家原理图、不拆主控固件的情况下，完成了三件事：

```text
1. 判断制冰机当前是待机、运行大冰，还是运行小冰
2. 模拟“开关”短按
3. 模拟“选择”短按，以及“选择”长按 5 秒开启/关闭 UV 杀菌
```

这篇文章面向不熟悉硬件和电气的读者，尤其是软件开发者。你可以把整件事理解成一次“硬件外设协议逆向”：我们先抓包，再建模，再构造请求，最后验证请求是否真的被设备接受。

## 最终结论先放前面

面板五根线命名为 `P1-P5`，固定网表如下：

```text
P5-P1 : LED5 + R5      冰满
P5-P2 : LED4 + R4      缺水
P1-P2 : SW1 + R6       开关
P1-P3 : SW2 + R7       选择
P3-P4 : R3 + LED3      大冰
P2-P4 : R2 + LED2      小冰
P1-P4 : R1 + LED1      电源
```

ESP32-C3 最终使用的接线：

```text
P1 -> GPIO0
P2 -> GPIO1
P3 -> GPIO2
P4 -> GPIO3
P5 -> GPIO4
```

厂家控制面板的扫描频率：

```text
约 13.03 ms 一帧
约 76.7 Hz
```

状态识别签名：

```text
运行大冰：0HHHH
运行小冰：MHMHH，但没有 4 秒电源灯慢闪
待机：MHMHH，并且有约 3.9-4.0 秒电源灯慢闪
```

最终可用控制命令：

```text
sw1_od 100          模拟开关键短按
sw2_od 80           模拟选择键短按
sw2_hold_od 5000    模拟选择键长按，切换 UV 杀菌
release             释放所有控制脚，恢复输入状态
```

这里的 `od` 是 open-drain，中文常叫“开漏”。简单理解：ESP32 只负责短暂把某根线拉低，然后放手，不主动推高。这比普通 GPIO 输出高/低电平更像真实按键，也更不容易和厂家主控硬碰硬。

## 故事从两张照片开始

用户最开始提供了制冰机控制面板电路板的两张照片：

```text
图一：电路板背面，能看到走线和电阻
图二：电路板正面，能看到 LED、按钮、5Pin 插座
```

已知面板上 5 个 LED 的含义：

```text
LED4：缺水
LED5：冰满
LED1：电源
LED2：小冰
LED3：大冰
```

按钮有两个：

```text
开关
选择
```

用户补充说：排线是 5 根线。

这就带来了第一个问题：为什么 5 根线能承载 5 个灯和 2 个按钮？

如果按最朴素的方式，每个 LED 一根控制线、每个按钮一根输入线，再加公共线，通常会需要更多线。但这块面板只有 5 根线，所以它几乎必然使用了“扫描复用”。

## 什么是扫描复用

用软件开发的语言讲，厂家没有给每个 LED 和按钮单独开一个 API，而是让它们共享一个小型总线。

主控大概在做类似这样的事情：

```text
while true:
    设置 P1-P5 的某种状态，点亮某个 LED 或检测某个按钮
    等待很短时间
    设置 P1-P5 的另一种状态
    等待很短时间
    重复
```

因为循环足够快，人眼会觉得 LED 是稳定亮着的。按钮也不需要一直检测，只要每隔十几毫秒扫一下，用户感受不到延迟。

本次实验后来证明，这个扫描周期大约是：

```text
13.03 ms
76.7 Hz
```

也就是说，厂家主控每秒大约轮询 76 次。

## 从照片到网表

照片只能给出线索。为了避免靠猜，用户后来手绘了一份电路图，我们一起校准。

最初手绘图里有一些支路方向和连接关系需要修正。用户进一步更正后，固定下来的网表如下：

```text
LED5 支路：
P5 -- LED5 -- R5 -- P1

LED4 支路：
P5 -- LED4 -- R4 -- P2

开关键支路：
P1 -- SW1(开关) -- R6 -- P2

选择键支路：
P1 -- SW2(选择) -- R7 -- P3

LED3 支路：
P3 -- R3 -- LED3 -- P4

LED2 支路：
P2 -- R2 -- LED2 -- P4

LED1 支路：
P1 -- R1 -- LED1 -- P4
```

这份网表非常关键。后面所有判断都围绕它展开。

例如：

```text
如果按下“选择”，P1 和 P3 应该产生明显相关变化
如果按下“开关”，P1 和 P2 应该产生明显相关变化
如果电源灯闪烁，P1-P4 应该有周期性变化
如果大冰灯亮，P3-P4 应该有特征变化
如果小冰灯亮，P2-P4 应该有特征变化
```

这就像逆向一个协议时，先推测字段含义，然后用抓包验证。

## 用户的万用表测量

用户在制冰机通电待机、未启动时，用万用表测了一组电压。现象是：

```text
电源指示灯缓慢闪烁
其他指示灯熄灭
部分 P 点之间电压稳定，部分随电源灯闪烁波动
最大看到约 4.7V
```

这组数据告诉我们两件事。

第一，这不是简单的静态电路。很多电压会波动，说明厂家在扫描或复用。

第二，ESP32-C3 直接接上去有风险。ESP32-C3 是 3.3V 芯片，GPIO 输入通常不能长期承受超过 `VDD + 0.3V` 的电压，也就是大约 3.6V。用户测到 4.xV，已经超过规格。

这里容易误解：4.7V 对人当然不危险，但对 3.3V 芯片的 GPIO 是可能危险的。它可能导致内部保护二极管导通，电流从面板倒灌进 ESP32，进一步影响 USB 甚至 Mac。

## 最初的安全方案

一开始的建议是不要直连，而是加保护前端：

```text
Pn -> 大电阻 -> ESP32 GPIO
GPIO 到 3V3/GND 加钳位二极管
GPIO 用弱偏置电阻固定在中间电位
ESP32 GND 不接制冰机
最好使用 USB 隔离器
```

这套方案的思路是：

```text
让 ESP32 只“闻一闻”信号，不让面板信号直接撞进 GPIO
```

但是实际进展中，用户已经把 P1-P5 直连到了 ESP32-C3 的 GPIO0-GPIO4，而且制冰机处于待机状态。用户希望在这个现实接法下继续实验。

于是我们改成了“直连 GPIO 版计划”。

## 直连 GPIO 版计划

直连方案的原则是：

```text
只做短时实验
ESP32 默认只输入
不连接制冰机 GND
Mac 尽量电池供电
先被动采集，再考虑主动模拟
```

这不是理想硬件方案，而是一个接受风险的调试方案。

软件上我们准备了两种采集模式：

```text
ADC 模式：
    连续读取 GPIO0-GPIO4 的模拟值
    输出 A,<us>,p1,p2,p3,p4,p5,mask

EDGE 模式：
    高速读取数字输入状态
    只在状态变化时输出 E,<us>,mask
```

为什么有两种？因为我们一开始不知道哪种会有效。

如果面板信号能被 ESP32 数字输入正确识别，EDGE 会很适合找扫描边沿。但如果没有共同地参考，数字输入阈值可能完全失效，此时 ADC 至少还能看到相对变化。

实际结果证明：

```text
EDGE 失败
ADC 成功
```

## 工具链和固件

我们使用：

```text
Arduino CLI 编译和上传 ESP32 固件
Python venv 管理采集脚本依赖
pyserial 读取串口
pandas/matplotlib 生成报告和图
```

ESP32-C3 Super Mini 这类板子有一个小坑：USB 串口输出需要编译参数：

```text
CDCOnBoot=cdc
```

否则程序烧录成功了，但 `Serial` 不一定从 USB-C 正常输出。

最终编译上传命令类似：

```sh
arduino-cli compile --clean --fqbn esp32:esp32:esp32c3:CDCOnBoot=cdc ice_panel_sniffer
arduino-cli upload -p /dev/cu.usbmodem11301 --fqbn esp32:esp32:esp32c3:CDCOnBoot=cdc ice_panel_sniffer
```

USB 端口也不是固定的。用户中途加了 USB-C 拓展坞，端口从早期的 `/dev/cu.usbmodem1101` 变成了后来的：

```text
/dev/cu.usbmodem11301
```

所以后续每次上传或采集前都会先枚举 `/dev/cu.usb*`。

## 第一次成功：待机电源灯慢闪

用户完成断电和刷机后，重新给制冰机上电进入待机。面板表现为：

```text
电源灯缓慢闪烁
其他灯熄灭
```

我们采集了第一组 ADC 数据：

```text
20260628-203622-direct_standby_adc
```

用户提到电源灯亮的时间很短，大约 1 秒，灭 3-4 秒，因此不方便人工精确标记“亮”和“灭”的瞬间。

这时就需要算法从数据里找周期。分析发现：

```text
P1 和 P1-P4 上有强周期
主周期约 3.99 秒
频率约 0.250 Hz
```

这和肉眼观察完全吻合。

这一步确认了两件事：

```text
1. ADC 虽然不是精确电压，但能抓到状态特征
2. P1-P4 确实对应电源 LED 的活动
```

## 第一次失败：EDGE 模式没有用

随后我们尝试 EDGE 模式：

```text
20260628-203923-direct_standby_edge
```

结果几乎没有有效边沿，`mask` 基本一直是：

```text
00000
```

这说明在“浮地直连”的接法里，ESP32 的数字输入阈值无法可靠解释这些节点。

这次失败很重要，因为它决定了后面策略：

```text
不要再把数字 mask 当主要依据
改用 ADC 读数和节点差分
```

## 手动按选择键：验证 SW2 是 P1-P3

接下来用户手动按“选择”键三次，每次 1-2 秒。采集文件：

```text
20260628-204123-direct_buttons_adc
```

分析发现，按选择时：

```text
P1-P3 的差值明显收敛
```

这正好符合网表：

```text
P1 -- SW2(选择) -- R7 -- P3
```

也就是说，选择键按下时，P1 和 P3 通过按键支路产生连接关系，ADC 上表现为两者更接近。

这一步确认：

```text
SW2 选择键 = P1-P3 支路
```

## 手动按开关键：长按失败，短按成功

用户随后手动按“开关”键。

第一次是长按 1-2 秒：

```text
20260628-204738-direct_power_button_adc
```

电气上我们确实检测到了 `P1-P2` 收敛：

```text
P1-P2 convergence at about 21.12-22.58 s
```

但制冰机没有启动。用户判断可能是按太长了，设备没有把它当成启动短按。

于是用户又做了一次短按：

```text
20260628-205011-direct_power_button_short_adc
```

这次成功启动。用户观察到：

```text
进入大冰模式
后来电源、大冰、缺水常亮
```

这一步确认：

```text
SW1 开关键 = P1-P2 支路
开关键启动需要短按，而不是 1-2 秒长按
```

也暴露了一个真实设备常见问题：按钮“按下”不是越久越好。厂家固件可能把不同长度的按压解释成不同含义，或者长按被忽略。

## 加水后重启：区分缺水亮和缺水灭

用户后来重启机器并加水，缺水灯熄灭，只剩：

```text
电源 + 大冰 常亮
```

采集文件：

```text
20260628-205853-direct_large_normal_adc
```

我们把它和之前“电源 + 大冰 + 缺水”进行对比。

结果有点遗憾：在当前直连浮地 ADC 方法下，缺水亮和缺水灭的主状态几乎一样。

大冰且缺水亮：

```text
主签名：0HHHH
```

大冰且缺水灭：

```text
主签名：0HHHH
```

虽然分布细节中 `P2` 和 `P5-P2` 有变化，但不够干净，不能可靠作为 LED4 缺水灯的独立判据。

这是第二个重要失败：

```text
直连浮地 ADC 足够识别大状态
但不足以可靠解析缺水/冰满这类故障 LED 的独立驱动窗口
```

如果要继续精确分析故障灯，需要更硬件化的前端，例如示波器、逻辑分析仪、保护电阻、钳位和偏置。

## 大冰和小冰的双向验证

接下来我们专门分析大小冰模式。

用户先从大冰切到小冰，采集文件：

```text
20260628-210541-direct_running_select_adc
```

电气上发现：

```text
大冰：0HHHH
小冰：MHMHH
```

其中切换发生在采集开始后约 11.25 秒。用户实际告诉我“选择完成，现在是小冰”的时间比电气切换晚一些，这说明人类消息时间不一定等于设备事件时间。后续分析都以设备数据为准。

然后用户又从小冰切回大冰，采集文件：

```text
20260628-211027-direct_running_select_back_adc
```

这次确认了反向映射：

```text
小冰：MHMHH
大冰：0HHHH
```

电气转折在约 93.55-93.65 秒之间，非常干净。

至此，大小冰状态被双向确认。

## 为什么待机和小冰会混淆

一个有趣问题出现了：

```text
小冰运行是 MHMHH
待机也经常是 MHMHH
```

如果只看主签名，就无法区分“待机”和“小冰运行”。

解决方法是看电源灯慢闪。

待机时：

```text
MHMHH 为主
P1 或 P1-P4 有约 3.9-4.0 秒周期
```

小冰运行时：

```text
MHMHH 为主
没有 4 秒电源灯慢闪周期
```

所以最终分类器不是简单的 `if signature == MHMHH`，而是：

```text
if 0HHHH 占比高:
    运行大冰
elif MHMHH 占比高:
    if P1/P1-P4 有约 4 秒慢闪:
        待机
    else:
        运行小冰
else:
    未知
```

后面用户让我判断“现在是否开机”，我们用这个分类器做了两次实际判断：

第一次判断为待机，用户确认正确。

第二次判断为运行大冰，用户确认正确。

## 讨论 USB 拓展坞是否更安全

中途用户在 Mac 和 ESP32 之间加了 USB-C 拓展坞，问是否更安全。

结论是：

```text
普通 USB-C 拓展坞不等于 USB 隔离器
```

它可能在物理接口上隔了一层，但通常不会提供真正的电气隔离。Mac、拓展坞、ESP32 的 USB 地线很可能仍然连在一起。

所以我们把风险判断分成两层：

```text
对 ESP32：风险较高，因为 GPIO 直接接面板
对 Mac：风险较低，但不是零
```

这也是为什么一开始我不愿意直接主动输出。直到被动分析充分验证后，才用更温和的开漏方式尝试模拟按键。

## 主动模拟选择键：先失败，再成功

用户明确要求尝试模拟短按“选择”。

一开始我拒绝直接普通 GPIO 输出，因为当前是直连、浮地、无保护。后来用户接受 ESP32 模块可能损坏的风险，并强调只要尽量保护 Mac。

于是我们采用分级策略：

```text
第一步：sw2_weak 120
第二步：sw2_od 80
必要时才考虑更强方式
```

`sw2_weak 120` 的含义：

```text
让 P3/GPIO2 只启用内部弱下拉 120 ms
```

这就像轻轻拉一下线。结果失败，没有切换。

采集文件：

```text
20260628-212854-direct_sw2_sim_weak_adc
20260628-212952-direct_sw2_sim_retry_adc
```

后来升级到：

```text
sw2_od 80
```

含义：

```text
让 P3/GPIO2 开漏拉低 80 ms
然后恢复所有 P1-P5 为浮空输入
```

这次成功。用户肉眼看到模式切换，ADC 也确认：

```text
大冰 0HHHH -> 小冰 MHMHH
```

随后我们又用同一个命令从小冰切回大冰：

```text
20260628-213241-direct_sw2_sim_back_adc
```

结果也成功：

```text
小冰 MHMHH -> 大冰 0HHHH
```

最终确认：

```text
sw2_od 80 = 选择键短按
```

## 主动模拟开关键：sw1_od 100 成功

接下来用户要求模拟开关键短按。

根据网表：

```text
开关键 SW1 = P1-P2
```

选择键成功时我们是拉 `P3`，所以开关键用同样思路，尝试拉 `P2`。

新增命令：

```text
sw1_od 100
```

含义：

```text
让 P2/GPIO1 开漏拉低 100 ms
然后恢复全输入
```

采集文件：

```text
20260628-213639-direct_sw1_sim_power_adc
```

用户确认：

```text
成功关机，进入待机
```

数据也显示：

```text
运行大冰 0HHHH -> 待机慢闪签名
```

最终确认：

```text
sw1_od 100 = 开关键短按
```

## 一次误操作：长按开关键 5 秒

后来用户要求“模拟长按 5 秒开关按钮”。我按照请求新增了：

```text
sw1_hold_od 5000
```

并实际执行了一次。

采集文件：

```text
20260628-214238-direct_sw1_hold_5s_adc
```

随后用户立刻更正：说错了，真正想要的是“长按选择键 5 秒”，因为那表示开启 UV 杀菌模式。

这个过程值得记录，因为它展示了实验协作中的真实情况：

```text
物理动作一旦执行，不能像 git commit 一样 revert
只能停止后续分析，重新按正确目标实验
```

所以后续文档中明确写了：

```text
sw1_hold_od 5000 是误请求期间加入的实验命令
不属于推荐远程控制接口
```

## 长按选择键：UV 开启和关闭

用户更正后，我们新增真正需要的命令：

```text
sw2_hold_od 5000
```

含义：

```text
让 P3/GPIO2 开漏拉低 5 秒
然后恢复全输入
```

第一次执行：

```text
20260628-214710-direct_sw2_hold_5s_uv_adc
```

用户确认：

```text
UV 开启
```

因为 UV 模式没有面板指示灯变化，所以 ADC 上没有明显状态切换。这很正常。我们只能确认两件事：

```text
1. ESP32 确实完整执行了 5 秒长按
2. 用户通过外部现象确认 UV 开启
```

随后用户要求再次长按 5 秒关闭 UV：

```text
20260628-214855-direct_sw2_hold_5s_uv_off_adc
```

用户确认：

```text
UV 关闭
```

最终确认：

```text
sw2_hold_od 5000 = 选择键长按，切换 UV 开/关
```

## 状态识别的最后验证

任务后期，用户问：

```text
现在是否处于开机状态，还是待机？
```

我们没有依赖用户肉眼描述，而是重新采样 35 秒：

```text
20260628-221419-current_state_check_adc
```

分析结果：

```text
MHMHH 占比约 74.5%
P1/P1-P4 有约 3.94 秒周期
判断：待机
```

用户确认正确。

后来用户又问：

```text
现在呢？
```

我们再采 25 秒：

```text
20260628-221633-current_state_check_2_adc
```

分析结果：

```text
0HHHH 占比约 83.4%
没有 4 秒慢闪
判断：运行大冰
```

用户再次确认正确。

这是整个实验闭环的最后一块：不仅能控制，还能可靠判断控制后的状态。

## 本次失败记录汇总

失败并不是坏事。相反，很多结论正是由失败限定出来的。

### 1. 数字 EDGE 模式失败

现象：

```text
mask 基本一直是 0
没有可用边沿
```

原因：

```text
浮地直连下，ESP32 数字输入阈值无法可靠解释面板节点
```

结论：

```text
不能用 digitalRead 做主要判断
改用 ADC 相对特征
```

### 2. `sw2_weak 120` 失败

现象：

```text
选择键弱下拉 120 ms 没有触发切换
```

原因：

```text
ESP32 内部弱下拉力量太小
厂家主控没有把它识别成真实按键
```

结论：

```text
需要开漏低脉冲 sw2_od 80
```

### 3. 开关键长按 1-2 秒没有启动

现象：

```text
手动长按开关 1-2 秒，电气上检测到 P1-P2 收敛，但机器没启动
```

原因推测：

```text
厂家固件只把短按当作开机
长按可能被忽略或被解释为其他操作
```

结论：

```text
模拟开关使用短脉冲 sw1_od 100
```

### 4. 缺水灯无法可靠解析

现象：

```text
缺水亮和缺水灭在主签名上都像 0HHHH
```

原因：

```text
直连浮地 ADC 饱和严重，且 LED 复用窗口可能被淹没
```

结论：

```text
当前方案不适合可靠识别缺水/冰满故障灯
```

### 5. 长按开关键是误目标

现象：

```text
用户后来更正，真正目标是长按选择键触发 UV
```

结论：

```text
sw1_hold_od 5000 保留为实验历史，不作为推荐控制命令
```

## 本次成功记录汇总

### 待机慢闪识别成功

```text
采集：20260628-203622-direct_standby_adc
结论：P1/P1-P4 有约 3.99 秒周期
意义：确认待机电源灯慢闪特征
```

### 选择键识别成功

```text
采集：20260628-204123-direct_buttons_adc
结论：按选择时 P1-P3 收敛
意义：确认 SW2 = P1-P3
```

### 开关键识别成功

```text
采集：20260628-204738-direct_power_button_adc
采集：20260628-205011-direct_power_button_short_adc
结论：按开关时 P1-P2 收敛，短按能启动
意义：确认 SW1 = P1-P2
```

### 大小冰识别成功

```text
采集：20260628-210541-direct_running_select_adc
采集：20260628-211027-direct_running_select_back_adc
结论：大冰 = 0HHHH，小冰 = MHMHH
```

### 选择短按模拟成功

```text
命令：sw2_od 80
结果：大小冰双向切换
```

### 开关短按模拟成功

```text
命令：sw1_od 100
结果：运行状态切到待机
```

### 选择长按模拟成功

```text
命令：sw2_hold_od 5000
结果：UV 开启，再次执行后 UV 关闭
```

### 状态分类验证成功

```text
20260628-221419-current_state_check_adc     判断待机，正确
20260628-221633-current_state_check_2_adc   判断运行大冰，正确
```

## 为什么 `0HHHH` 和 `MHMHH` 能当状态码

ADC 原始读数比较混乱，因为没有共同地参考，而且很多值会饱和到 0 或 4095。

但是混乱里有稳定结构。我们把每个 P 点粗分桶：

```text
0 : ADC < 100
M : ADC 大约 1500-2500
H : ADC > 3995
x : 其他
```

然后按 `P1 P2 P3 P4 P5` 拼成签名。

例如：

```text
0HHHH
```

表示：

```text
P1 很低
P2/P3/P4/P5 很高
```

它稳定出现在大冰运行状态。

再比如：

```text
MHMHH
```

表示：

```text
P1/P3 中间
P2/P4/P5 很高
```

它出现在小冰运行和待机中，所以要结合慢闪周期进一步判断。

这就像软件里对日志做特征压缩：原始日志噪声很大，但压缩后的模式能稳定分类。

## 为什么主动控制用开漏

普通 GPIO 输出有两种动作：

```text
输出高：主动推到 3.3V
输出低：主动拉到 0V
```

问题是制冰机主控也在驱动这些线。如果 ESP32 输出高，而厂家主控想拉低，就会产生冲突。

开漏输出更温和：

```text
需要按键时：拉低
不需要按键时：放手，恢复输入
```

它更像真实按钮，因为按钮本质上不是输出电压，而是短暂改变两个节点之间的连接关系。

这也是为什么：

```text
sw2_weak 120 太弱，失败
sw2_od 80 足够像真实按键，成功
sw1_od 100 足够像真实按键，成功
```

## 仍然存在的硬件风险

这次实验成功，不代表直连方案适合长期使用。

风险点：

```text
ESP32-C3 是 3.3V 芯片，面板出现过 4.xV 节点差
ESP32 GND 没有接制冰机，ADC 只能当相对特征
普通 USB-C 拓展坞不是电气隔离器
主动模拟按键仍然直接碰到了面板线
```

更安全的工程化做法是：

```text
按钮模拟：
    使用 PhotoMOS、干簧继电器、继电器、光耦模拟开关等隔离触点

状态采集：
    使用高值串联电阻、钳位二极管、弱偏置
    或者使用隔离示波器/逻辑分析仪前端

软件层：
    把状态识别和控制命令封装成状态机
    防止重复发送、误发送、长时间拉线
```

可以把本次直连方案理解为：

```text
实验室调试探针
```

而不是：

```text
长期可靠产品接口
```

## 术语表

| 术语 | 通俗解释 |
|---|---|
| 节点 | 电路中的连接点，本项目里就是 P1-P5 |
| 网表 | 元件连接关系清单，类似依赖图 |
| GPIO | ESP32 可编程输入输出脚 |
| ADC | 把模拟电压转换成数字读数 |
| 浮地 | ESP32 和制冰机没有共同 0V 参考 |
| 高阻输入 | 尽量只观察，不影响原电路 |
| 开漏 | 只拉低，不主动推高；更适合模拟按键 |
| 扫描 | 主控快速轮流驱动和读取节点 |
| 复用 | 用少量线完成多个功能 |
| 签名 | ADC 粗分桶后的状态码，比如 0HHHH |
| 占空比 | 某状态在周期中出现的时间比例 |
| 慢闪周期 | 待机电源灯约 4 秒一次的闪烁 |
| 钳位 | 用保护器件限制电压范围 |
| 弱下拉 | 用很弱的内部电阻把信号拉向低电平 |
| 开漏低脉冲 | 短暂把某线拉低，然后放手 |

## 最后的工程总结

本次逆向不是靠一次灵感完成的，而是逐步收敛：

```text
照片识别元件
手绘并校准网表
万用表确认电压和波动
ESP32 被动采集 ADC
发现 EDGE 失败，转向 ADC 特征
手动按键验证 P1-P2/P1-P3
手动切换验证大冰/小冰签名
主动开漏脉冲模拟选择键
主动开漏脉冲模拟开关键
长按选择键验证 UV 开关
最后用状态分类器做盲测，用户确认两次正确
```

如果用一句话概括：

```text
我们把一个没有文档的五线面板，逆向成了一个可识别、可控制的简单协议。
```

最终推荐给后续软件控制使用的接口只有三个：

```text
sw1_od 100          开关短按
sw2_od 80           选择短按
sw2_hold_od 5000    选择长按，切换 UV
```

最终推荐的状态判断规则是：

```text
0HHHH 主导，无 4 秒慢闪：运行大冰
MHMHH 主导，有 4 秒慢闪：待机
MHMHH 主导，无 4 秒慢闪：运行小冰
其他：未知或过渡状态
```

这份规则不是从厂家文档抄来的，而是从一次次采集、失败、修正和验证里长出来的。
